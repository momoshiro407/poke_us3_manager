<h1>育成データ編集</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>
    <%= form_for @untrained_monster, url: {action: :update} do |f| %>
      <%= render 'form', f: f %>
      <%= f.submit '更新', class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>