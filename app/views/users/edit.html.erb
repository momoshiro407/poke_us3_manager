<%= provide(:title, 'Edit User') %>
<h1>ユーザープロフィール更新</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= render 'form', f: f %>
      <%= f.submit '変更内容を保存', class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>